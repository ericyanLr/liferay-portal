import com.liferay.gradle.plugins.patcher.PatchTask

apply plugin: "com.liferay.patcher"

task patch(type: PatchTask)

dependencies {
	compileOnly group: "javax.servlet.jsp", name: "javax.servlet.jsp-api", transitive: false, version: "2.3.3"
	compileOnly group: "org.glassfish", name: "javax.el", transitive: false, version: "3.0.1-b12"
}

jarSources {
	mustRunAfter patch
}

patch {
	ext {
		autoClean = false
	}

	fileNames "javax/servlet/jsp/el/ScopedAttributeELResolver.java"

	originalLibModuleName = "javax.servlet.jsp-api"
}